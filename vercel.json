{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "functions": {
    "api/[...route].ts": {
      "maxDuration": 30,
      "includeFiles": ".serverless/**"
    }
  },
  "crons": [
    {
      "path": "/api/scheduler/cron",
      "schedule": "0 0 * * *"
    }
  ]
}
